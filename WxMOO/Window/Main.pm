package WxMOO::Window::Main;
use perl5i::2;

use parent 'WxMOO::Window';

method new($class: %args) {
    my $self = $class->SUPER::new( %args );

    return $self;
}

"The pain in Main is straining in my brain.";
